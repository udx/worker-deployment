# worker-config

Generates a `deploy.yml` template in the current directory.

## Usage

```bash
worker config
```

## Options

- `--output=FILE` output file (default: `deploy.yml`)
- `--force` overwrite existing file without prompt

## npx

```bash
npx @udx/worker-deployment worker config
```
